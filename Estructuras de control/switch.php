<?php
switch ($i) {
    case 0:
        # Imprime el valor con el que la variable $i llegó al switch
        echo "i es igual a 0";
    case 1:
        # Imprime el valor con el que la variable $i llegó al switch
        echo "i es igual a 1";
    case 2:
        # Imprime el valor con el que la variable $i llegó al switch
        echo "i es igual a 2";
}
?>